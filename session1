# ~/.tmux/session1

# Create a new session named "session1"
new-session -d -s session1

# Split the window into two panes horizontally
split-window -h

# Run a command in the first pane
send-keys 'btop' C-m

# Split the second pane vertically
# split-window -v

# Run a command in the new pane
# send-keys 'htop' C-m

# Create a new window named "logs"
new-window -n netflix

# Run a command in the first pane of the "logs" window
send-keys 'netflix && up' C-m

# Attach to the "session1" session

# attach-session -t session1
